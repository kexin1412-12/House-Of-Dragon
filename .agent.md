# AI 导演注 Agent

## 核心原则

1. **防剧透是检索层硬约束，不是 LLM 的自觉**
   所有查询都带 `cursor_time`，KB 在检索阶段就过滤掉 `start_time > cursor_time` 的片段。Agent 在上下文里根本看不到未来内容，不需要 prompt 叮嘱"不要剧透"。

2. **被动触发 与 主动问答 走两条独立链路**
   - 被动（时间线侧栏卡片）：直查预生成数据，**不经过 LLM**，快、稳、便宜。
   - 主动（用户问答）：才进入 Agent，检索 + 生成。

3. **伏笔双向索引，单向呈现**
   离线建立 `foreshadow_id ↔ payoff_id` 链接；在线呈现时 setup 端只说"注意这里"，payoff 端才说"还记得吗"。永不同时暴露两头。

4. **单 Agent + Retrieval Tools，不做多 Agent 调度**
   镜头 / 剧情 / 伏笔 / 人物 = 4 个检索工具，不是 4 个子 Agent。LLM 自己决定调哪个。

5. **Schema 才是架构，Agent 只是壳**
   把每个 scene 节点的字段定好，90% 的工作就完成了。

## 架构

```
前端播放器
├── 被动链路：GET /api/agent/cards?videoId=X&t=123
│   └── 直查预生成 KB → 返回卡片数组（无 LLM）
└── 主动链路：POST /api/agent/chat { videoId, t, question }
    └── 带 cursor_time 的 spoiler-safe 检索 → LLM 生成回答
```

## 视频 KB Schema

```jsonc
{
  "video_id": "sample",
  "duration": 180,
  "scenes": [
    {
      "scene_id": "s1",
      "start_time": 0,
      "end_time": 45,
      "shot": {
        "framing": "中景",       // 景别
        "camera": "缓慢推镜",     // 运镜
        "light": "冷色调逆光",    // 光线
        "intent": "营造疏离感"    // 构图意图
      },
      "plot": {
        "fact": "主角在雨夜回到老宅",         // 事实层（永远可说）
        "reading": "暗示对过去的不甘"         // 解读层（可能带剧透倾向）
      },
      "characters": [
        { "id": "林远", "emotion": "压抑", "motivation_shift": "开始动摇" }
      ],
      "foreshadow": {
        "is_setup_for": ["s7"],           // 这一场的细节会在 s7 回扣
        "is_payoff_of": [],                // 这一场也回扣了哪些更早的场景
        "setup_hint": "注意桌上那封没拆的信"  // 只能在 setup 时说的轻提示
      },
      "spoiler_level": 0                   // 0=任意可说 1=当前时间后可说 2=结局级
    }
  ]
}
```

**关键字段说明**：

| 字段 | 作用 |
|---|---|
| `spoiler_level` | 检索层按 cursor_time 过滤的第二重保险 |
| `foreshadow.setup_hint` | setup 端的轻提示，**绝不能提 payoff_id 指向的内容** |
| `plot.fact` vs `plot.reading` | fact 永远可说，reading 可能需要等到 payoff 后才能完整给出 |

## 离线预处理

视频上传后：场景切分 → 多模态分析（镜头/剧情/人物）→ 写入 `server/kb/<video_id>.json`。

Demo 阶段可以：
- 手工标注一两个视频作为演示素材
- 或用一次性批处理 LLM（给完整视频 + prompt 模板）生成 KB

## Agent 工具（都带 cursor_time）

| 工具 | 返回 |
|---|---|
| `get_shot_analysis(t)` | 当前 scene 的 shot 字段 |
| `get_plot_context(t)` | `scenes[].plot.fact` where `start_time <= t` |
| `get_foreshadow_hints(t)` | 当前 scene 的 setup_hint（永不返回 payoff 内容） |
| `get_character_state(t)` | 当前 scene 的 characters 快照 |

## 触发示例

- **被动**：`t=00:15:30` 进入新 scene → 推送镜头卡片 + 可能的 setup_hint
- **主动**：
  - "刚才那个绿色火光什么意思？" → `get_shot_analysis` + `get_foreshadow_hints`
  - "她为什么沉默？" → `get_character_state` + `get_plot_context`
  - "我刚才错过了什么？" → `get_plot_context`（取最近 2-3 个 scene 的 fact）

## 不做什么

- 不直接分析原始视频流，只读 KB
- 不做实时视频理解（离线预处理一次）
- 不做多 Agent 协同（单 Agent + 工具即可）
- 不依赖 prompt 防剧透（由检索层硬保证）
